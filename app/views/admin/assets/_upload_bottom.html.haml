- # encoding: utf-8
- unless @assets_image_list == "tags"
  #actions
    = pagination_for @assets, :depaginate => false
    %ul
      %li= link_to image('plus') + " " + t('paperclipped.new_asset'), new_admin_asset_path
      - if allowed_by_role?
        %li= link_to image('plus') + image('plus') + " " + t("paperclipped_uploader.bulk_uploader"), upload_assets_path
